This is where we add stuff we did and things to do...

TO DO FOR EVERYONE:
- Alter the svg files of the maps so that each section is compartmentalized into different nodes (for instance, mezzanine is broken into
different nodes based on exits and wall separations, platforms are broken to different nodes relative to the head/middle/rear of train)
- Simplify nodes selection to include only exit stairs (remove other stairs) and the mezzanine/platform nodes


Edmond ----
TO DO:
(VERY IMPORTANT RIGHT NOW) Fix API url routing for the fetching edges and nodes. Either choose to use a hybrid field
with slugs and ids together or just use ids in the API routing + user routing.
Connect second page (station selections page) seamlessly to the third page (ivan's page)
Find a way to properly cache requested database data to avoid excessive queries 
Do a visual rework and logical refactor of the first and second page (almost done with first page ~90%)

DONE:
Add additional functions to data_fetch class
Register data fetching functions (frontend and backend) for station line tables
Refactor existing pathfinder/app class
    - Make the code moore compartmentalized (unspaghetti the spaghetti code)
Finish frontend function to fetch stations, line, and related metadata (see data_fetch.ts)
Create separate web pages for user input 
    - initial page to show all available subway lines (***done at this point)
    - second page to show all available stations given that line selected (**)
    - provide proper transfer of data in coresponding pages (via query params and session storage) 

Ivan ----
TO DO:
(Priority)
Create new model for station layers
    - Fields: id, name, layerOrder (lowest is on the bottom), color, stationId, svgId
    - Modify nodes model to contain layerId rather than layer svgId as a foreign key
Port new station maps to the database with more nodes
Edit station map page to show layers selection as a side panel instead of horizontal bar
When user selects start and end from the dropdown, the chosen nodes will flash before route is calculated

(Non-priority)
The pan/zoom feature for the station map is quite small for smaller screen sizes and limits the zoom a bit too much
Create a new test page to navigate between two different stations (This page is for integration with Google Maps)
    - The next button doesn't work when boarding the train. (Might want to change the
        appearance of the next button at this stage or make the button do the same 
        the board train button)
    - (Potential) Make the phase tracker interactable to move to different phases
Need to think about how edges work since there are escalators that only go one direction. Additionally
elevators go to multiple layers.

DONE:
Fix Center node to work on any viewport size not just on the full_path page
Created a landing page on index view that you can get to discover/lines from
Make the third page for displaying individual stations
    - On the top there is a horizontal bar with radio buttons matching the 
    number of layers (plus a button for the whole map) that the station have. Each segment
    corresponds to a layer and click on the section toggles it and shows
    that specific layer of the map
    - Show a zoomable and movable station map
    - Uses new fetching api from the database
    - Users can generate a path between two nodes within the stations
    - Highlighted nodes are centered on (need to port from full_path.ts or make
    a new ts file so both pages import from the same page?)
Create a new test page to navigate between two different stations (This page is for integration with Google Maps)
    Issues Fixed:
    - Highlight on svg not working
    - Unable to go back after boarding the train to next station
    - At the end of final station, it shows the board train thing when
      it shouldn't since the trip has ended
    - Need to make the image of the svg interactable to zoom in and out
      along with moving around in the map
    - Top bar doesn't work (need to dynamically modify based on stations that
      are being shown)
Add 25 ave to the seed
Refactor the showLayer to pass in all layers on the station
    - serialize list of all unique layers from database
    - iterate through the StationResponse to find unqiue layers in js
Replace Old Bay 50 st with new one
    - change svg path
    - update seeds for bay 50 st
Make the test form for the find path auto generate options based on database
Connected fetching function to find path function in the TypeScript
