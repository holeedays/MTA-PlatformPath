This is where we add stuff we did and things to do...

Edmond ----
TO DO:
Register data fetching functions (frontend and backend) for station line tables
Finish frontend function to fetch stations, line, and related metadata (see data_fetch.ts)


DONE:



Ivan ----
TO DO:
Refactor the showLayer to pass in all layers on the station
    - serialize list of all unique layers from database
    - iterate through the StationResponse to find unqiue layers in js
Replace Old Bay 50 st with new one
    - change svg path
    - update seeds for bay 50 st
Add 25 ave to the seed

DONE:
Make the test form for the find path auto generate options based on database
Connected fetching function to find path function in the TypeScript
